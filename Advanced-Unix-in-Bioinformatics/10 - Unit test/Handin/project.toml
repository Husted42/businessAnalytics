[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fasta-project"
version = "0.1.0"
description = "FASTA class for bioinformatics exercises"
requires-python = ">=3.8"

[tool.setuptools]
py-modules = ["fasta"]
package-dir = {"" = "src"}